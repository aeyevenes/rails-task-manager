<br>
<br>
<br>
<div class="banner">
    <h1>Our Tasks</h1>
     <ul id="tasks">
     <% @tasks.each do |task| %>
    <li>
    <p>
      <% if task.completed %>
        <i class="fa fa-check-square-o"></i>
      <% else %>
        <i class="fa fa-square-o"></i>
      <% end %>
      <%= link_to task.tittle, task_path(task.id) %>
      ·
      <%= link_to task_path(task.id), method: :delete, data: { confirm: "Delete #{task.tittle}?" } do %>
        <i class="fa fa-trash-o" aria-hidden="true"></i>
      <% end %>
      </p>
    </li>
  <% end %>
</ul>

<hr>
<div class= banner p>
<%= link_to new_task_path do %>
  <i class="fa fa-plus"></i> <h1> Add a new task </h1>
<% end %>
